{
  "name": "upload-to-firebase-api",
  "version": "1.0.0",
  "type": "module",
  "description": "this is the backend for the test project to push images to firebase ad storing the url to another postgres url database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ahmednule",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "apollo-server-express": "^3.13.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "firebase-admin": "^13.2.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
